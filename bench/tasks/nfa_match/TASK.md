Implement `fullmatch(pattern, text)` in `nfa.py` exactly as `SPEC.md`
describes.

`python3 test_nfa.py` is three cases. The hidden check is several hundred
pattern/text pairs, including alternation, nested groups, and empty
repeats. You may not use the `re`, `regex` or `fnmatch` modules.

Do not modify the test or the spec.
