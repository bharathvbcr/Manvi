`python3 test_intervals.py` fails.

Fix `intervals.py` so `merge`, `intersect` and `subtract` all satisfy `SPEC.md`
exactly. The visible test covers four easy cases; your implementation will be
checked against many more, including unsorted input, empty intervals, touching
intervals, containment, negative coordinates and empty lists.

Do not modify the test or the spec.
