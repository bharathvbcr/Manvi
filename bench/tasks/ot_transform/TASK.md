Implement `apply` and `transform` in `ot.py` exactly as `SPEC.md` describes.

`python3 test_ot.py` is two inserts that do not overlap. The hidden check is
the convergence property on concurrent inserts, deletes, and same-position
ties, including no-ops.

Do not modify the test or the spec.
