Implement `parse(expr)` in `parse.py` exactly as `SPEC.md` describes.

`python3 test_parse.py` covers two arithmetic cases. The hidden check covers
unary minus vs power, right-associative `^`, postfix `!`, integer division
toward zero, and a pile of syntax errors.

Do not modify the test or the spec.
